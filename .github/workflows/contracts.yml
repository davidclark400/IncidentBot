name: Contract synchronization

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.301

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: src/Panko.Client/package-lock.json

      - name: Install TypeScript generator
        run: npm ci
        working-directory: src/Panko.Client

      - name: Generate OpenAPI and TypeScript contracts
        run: dotnet build src/Panko.Api/Panko.Api.csproj -p:BuildClient=false

      - name: Verify generated contracts are committed
        run: |
          npm run contracts:check --prefix src/Panko.Client
          git diff --exit-code -- src/Panko.Client/openapi src/Panko.Client/src/api-client
