version: 2
revision: "kafka-fixture.1"
fallbackSlackChannel: "#incidentbot-fixture"

profiles:
  - id: kafka-synthetic-fixture
    pagerDutyServiceId: PKAFKAFIXTURE
    team: platform
    slackChannel: "#incidentbot-fixture"
    kafka:
      metricPackId: synthetic-fixture-kafka-v1
      cluster: fixture-cluster
      topics:
        - fixture-orders
      consumerGroups:
        - fixture-workers
